[build]
  command = "pnpm build"
  publish = ".output/public"

[build.environment]
  NITRO_PRESET = "static"
  NODE_VERSION = "22"
  # VITE_API_BASE_URL and STRAPI_API_TOKEN must be set in Netlify UI:
  # Site → Site configuration → Environment variables
